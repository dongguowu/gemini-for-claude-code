# Required: Provide your Google AI Studio API keys here.
# You can use the new API_KEYS variable (recommended) or the deprecated GEMINI_API_KEY.
#
# API_KEYS (Recommended): A JSON array of your API keys.
# Example: API_KEYS='["key1", "key2", "key3"]'
API_KEYS='["your-google-ai-studio-key-1", "your-google-ai-studio-key-2"]'

# GEMINI_API_KEY (Deprecated): A single key or a comma-separated list of keys.
# This will be removed in a future version.
GEMINI_API_KEY=""

# Optional: Model mappings for Claude Code aliases
BIG_MODEL="gemini-2.5-pro"      # For 'sonnet' or 'opus' requests (latest stable)
SMALL_MODEL="gemini-2.5-flash"  # For 'haiku' requests (latest stable, best price-performance)

# Optional: Server settings
HOST="0.0.0.0"
PORT="8082"
LOG_LEVEL="WARNING"  # DEBUG, INFO, WARNING, ERROR, CRITICAL

# Optional: Set a custom endpoint for Gemini API (e.g., a proxy)
# GEMINI_API_BASE="https://your-proxy-url/v1"

# Optional: Performance and reliability settings
MAX_TOKENS_LIMIT="8192"           # Max tokens for Gemini responses
REQUEST_TIMEOUT="90"              # Request timeout in seconds
MAX_RETRIES="2"                   # LiteLLM retries to Gemini
MAX_STREAMING_RETRIES="12"         # Streaming-specific retry attempts

# Optional: Streaming control (use if experiencing issues)
FORCE_DISABLE_STREAMING="false"     # Disable streaming globally
EMERGENCY_DISABLE_STREAMING="false" # Emergency streaming disable
